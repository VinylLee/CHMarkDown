# 待办

待办条目已整理进 `ROADMAP.md` 并按版本排期，开发时以路线图为准。

## v1.0.7–v1.1.0 已完成

- 默认打开模式只在程序启动时生效，切换文档时保持当前视图模式
- 编辑区内容未填满一页时也可滚轮下滑，滚到底时最后一行位于顶部、下方留白
- 修复长文档预览定位与查找跟随滚动（v1.0.8）
- 选区成对符号包裹与 `Ctrl+G` 大纲快捷键（v1.0.9）
- 打开并按纯文本预览 TXT/JSON 文件（v1.0.10）
- 优化数千行 Markdown 的输入、派生计算与双向滚动响应（v1.1.0）

## 后续待办

当前没有已排期的后续版本；新增需求先更新 `ROADMAP.md` 再开发。
