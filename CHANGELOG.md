# CHMarkDown 更新日志

本文记录 CHMarkDown 面向用户的重要变化。版本号遵循语义化版本规则。

## [1.0.5] - 2026-08-05

### 编辑器

- 新增字数统计：编辑区与预览区标签栏实时显示当前文档字数，中文按字符计数、英文按单词计数，Markdown 语法符号和链接地址不计入。

### 验证

- 新增字数统计单元测试，全部 332 项自动化测试通过，TypeScript 类型检查和生产构建通过。

## [1.0.4] - 2026-08-05

### 预览与大纲

- 纯预览模式的正文不再受固定 820px 宽度限制，会随窗口大小同步放大和缩小。
- 文档大纲支持折叠/展开：带子标题的条目显示箭头，点击箭头只收起或展开该标题下的子标题，其他条目不受影响；点击标题仍可定位跳转。
- 按住 Shift 点击大纲折叠箭头时，会对同一层级的所有标题执行相同的展开/收起操作。

### 文档列表

- 最近文件区域与文档列表之间新增可拖拽分隔条，上下拖动即可调整最近文件高度，调整结果会保留。

### 验证

- 新增大纲树构建、折叠可见性、同层级折叠和纵向拖拽测试，全部 325 项自动化测试通过，TypeScript 类型检查和生产构建通过。

## [1.0.3] - 2026-08-05

### 图片

- 图片尺寸调整面板新增数值输入框（10%–100%），拖动滑块或输入数值时预览中的图片即时更新，调整过程中图片保持可见；100% 等三位数值可完整显示。
- 外部 Markdown 文件中的相对路径图片现在同样支持点击预览调整尺寸。
- 点击预览中的图片后按 Delete 或 Backspace 可以直接删除该图片。
- 确认"默认打开模式"的默认值为仅预览：新安装用户默认进入预览视图；该设置可在偏好设置中修改为仅编辑、分栏或仅预览。

### 验证

- 新增默认视图默认值、外部文件相对图片尺寸和图片删除测试，全部 304 项自动化测试通过，TypeScript 类型检查和生产构建通过。

## [1.0.2] - 2026-08-05

### 编辑器

- 列表自动续行：在 `- `、`* `、`1. ` 等列表项末尾按回车，新行自动带上相同的列表符号；有序列表编号自动递增。
- 在空的列表项（仅含列表符号）按回车，删除该行列表符号并结束列表；普通段落的回车行为不受影响。
- 光标位于列表符号前按回车时只插入空行，不会误新增列表项。
- 新增 `Ctrl+Enter` 快捷键：无论光标在何处，先跳到当前行行末再执行回车（列表行继续列表，普通行插入换行）。
- 图片尺寸调整面板在拖动滑块或点击预设尺寸时保持可见，不再因调整操作而意外关闭；调整完成后仍可正常关闭。

### 验证

- 新增列表续行单元测试，覆盖无序、有序、缩进列表、行中拆分、空列表项退出和边界情况。
- 全部 293 项自动化测试通过，TypeScript 类型检查和生产构建通过。

## [1.0.1] - 2026-08-03

### 编辑器

- 编辑区没有选中文字时，`Ctrl+C` 复制完整当前行，`Ctrl+X` 剪切完整当前行。
- 由上述方式复制或剪切的行，在无选区时按 `Ctrl+V` 会插入到当前行上方，并保持原光标列位置。
- 使用专用剪贴板行标记区分整行操作；普通文本、有选区内容和其他应用复制的内容不改变原有粘贴行为。
- 补充首行、中间行、末行、空末行和无尾换行文档的边界测试。
- 新增 `Ctrl+Z` 撤销与 `Ctrl+Y` 恢复，保留最多 100 步正文编辑历史，并同步恢复光标和选区。
- 连续输入和同方向删除会在 750 ms 内合并；剪贴、替换及图片相关修改作为独立步骤，新修改会清除恢复分支。

### 文件拖放

- 修复 Electron 43 中拖入 Markdown 文件提示“无法取得拖入文件的本地路径”的问题。
- 改用 preload 内的 `webUtils.getPathForFile`，支持中文和空格目录且不降低 Electron 安全配置。

### 图片

- 本地笔记与外部 Markdown 新插入、粘贴的图片统一使用 `![图片](...)` Markdown 语法。
- 图片尺寸调整继续支持 10%–100%，并使用 `{width=N%}` 后缀保留尺寸。
- 已有 `<img src="chmarkdown://images/...">` 内容继续正常预览；调整尺寸时自动迁移为 Markdown。
- 导出时把旧 HTML 和新版受管图片统一转换为 ZIP 内的相对 Markdown 图片引用，修复旧格式无法得到标准 Markdown 图片语法的问题。

### 最近文件

- 点击“最近文件”标题即可折叠或展开列表，标题显示记录数量和方向状态。
- 折叠按钮支持键盘操作和 ARIA 展开状态，最后状态会在重启后恢复。

### 验证

- 新增行式剪贴板、编辑历史、拖放路径、两种图片格式统一导出和最近文件折叠持久化测试。
- 32 个测试文件、248 项自动化测试全部通过。
- TypeScript 类型检查和生产构建通过。

## [1.0.0] - 2026-08-03

CHMarkDown 1.0.0 是第一个正式稳定版。它将此前分阶段完成的本地笔记、外部
Markdown 文件、图片、导出、会话恢复、偏好设置和系统托盘能力整合为一个可直接
分发的 Windows x64 应用。本版本不增加账号、云同步、后台服务或遥测。

### 正式版重点

- 同时支持应用内本地笔记和磁盘上的 `.md` / `.markdown` 文件。
- 支持打开、编辑、保存、另存为、最近文件、拖放和 Windows“打开方式”。
- 自动恢复上次打开的文档、排列顺序和当前选中项；失效文件只影响自身。
- 提供 Markdown 实时预览、三种显示模式和编辑/预览双向定位。
- 切换编辑、分栏、预览模式时保持阅读位置、光标和选区。
- 支持当前文档查找、替换、大小写/全词匹配以及 Markdown 标题大纲。
- 支持本地图片插入、粘贴、尺寸调整和外部 Markdown 相对图片预览。
- 统一导出 Markdown：没有图片时生成 `.md`，包含图片引用时生成带资源的 `.zip`。
- 提供浅色、深色、跟随系统、字体、字号、自动换行和默认模式等本地偏好设置。
- 支持系统托盘常驻；也可以关闭托盘，使关闭窗口直接退出应用。
- 分栏模式的编辑区和预览区宽度可以拖动、键盘调整、双击复位并持久保存。
- 应用全局保持单实例；重复启动时恢复并聚焦已有窗口，系统传入的文件交给该窗口打开。

### 自 v0.4.3 以来的新增内容

#### 图片与导出（v0.5.x）

- 外部 Markdown 的相对图片通过受限自定义协议安全预览。
- 新插入或粘贴的图片复制到文档旁可配置的资源目录，正文使用相对路径。
- 另存为时同步复制关联图片目录，并阻止静默覆盖已有资源目录。
- 本地笔记和外部文件使用同一个导出入口。
- 导出前检查未保存修改；缺失、越界或无法读取的本地图片会明确报错。
- ZIP 中的 Markdown 引用会改写为可移植路径，重复图片只打包一次，远程图片保留 URL。

#### 编辑器偏好设置（v0.6.0）

- 新增主题、编辑器字体、字号、默认显示模式、自动换行和图片资源目录设置。
- 设置写入本地 `settings.json`，保存后立即生效，重启后继续保留。
- 设置损坏或内容不合法时使用安全默认值，并向用户显示提示。

#### 系统托盘（v0.7.0）

- 默认显示 CHMarkDown 托盘图标，关闭主窗口时隐藏到托盘。
- 托盘单击和菜单均可恢复同一个窗口；托盘菜单支持明确退出。
- 关闭托盘选项后，关闭窗口会正常退出应用。
- 真正退出仍经过未保存修改和最终会话写入检查；系统注销或关机不会被阻止。

#### 可调整分栏（v0.8.0）

- 编辑区与预览区之间的分隔线支持鼠标左右拖动。
- 编辑区比例限制在 20%–80%，避免任意一侧被完全隐藏。
- 支持左右方向键微调、Shift 加速调整和双击恢复 50:50。
- 分栏比例保存在本机，切换文档、模式或重启应用后继续使用。

#### 启动性能与单实例（v0.9.0）

- 文档导出服务和非首屏界面改为按需加载。
- 主窗口优先创建，应用菜单和托盘随后初始化。
- 设置只在启动时读取并校验一次，后续操作复用内存缓存。
- 笔记与会话并行读取，多个外部文件并行恢复且保持原有排列顺序。
- 解压版同机 5 次测试的启动中位数由 0.484 秒降至 0.329 秒，改善约 32%。
- 新增可重复运行的启动测量脚本和隔离测试数据目录。
- 重复启动只保留一个应用窗口；隐藏或最小化的窗口会被恢复并聚焦。

### 1.0 稳定性与安全维护

- Electron 从 28 更新到 43.2.0，继续保持 `contextIsolation: true` 和
  `nodeIntegration: false`。
- electron-builder 更新到 26.15.3；Vite 更新到 8.2.0，Vitest 更新到 4.1.10。
- 移除不再需要的 DOMPurify 外部类型占位包，继续使用 DOMPurify 自带类型。
- 更新间接依赖后，npm 官方完整依赖审计结果为 0 个已知漏洞。
- 增加超过 8 MiB 的 UTF-8 Markdown 文件读写回归测试，验证中文、emoji 和代码文本
  不被截断或改写。
- 使用锁定依赖完成全新安装，确保构建可以从干净环境复现。

### 数据与兼容性

- 继续使用本地 `notes.json`、`recent-files.json`、`session.json`、`settings.json` 和
  应用图片目录；从 0.9.0 升级不需要数据迁移。
- 外部 Markdown 只有在用户明确保存或另存为时才会写入磁盘。
- 发行包自带完整 Electron 运行时，不依赖系统预装 Node.js、Edge、Chrome 或 WebView2。
- 本版本面向 Windows x64，提供单文件便携版和解压后运行的 ZIP 版。
- 发行包未使用商业代码签名证书；Windows 首次运行时可能显示 SmartScreen 提示，
  可使用发布页提供的 SHA-256 校验文件完整性。

### 测试与发行验证

- 自动化测试：29 个测试文件，共 226 项测试全部通过。
- TypeScript 类型检查和 Vite 生产构建通过。
- npm 官方完整依赖审计：0 个已知漏洞。
- Windows x64 便携版和 ZIP 版均完成构建、包内版本及资源结构检查。
- 便携版实际启动、单实例重复启动、窗口恢复和退出烟雾测试通过。
- 1.0.0 便携版 5 次启动为 1.169、1.100、1.138、1.043、1.259 秒，
  中位数 1.138 秒。
- 1.0.0 解压版 5 次启动为 0.283、0.236、0.386、0.391、0.404 秒，
  中位数 0.386 秒。
- 相比使用 Electron 28 的 0.9.0，Electron 43 的运行时体积和启动时间略有增加；
  本版本优先采用仍处于安全维护范围内的运行时。

### 发行文件

| 文件 | 大小 | SHA-256 |
|------|------|---------|
| `CHMarkDown-1.0.0-portable-x64.exe` | 327.46 MiB | `C85A95F8C3B1715CD690EAF88FA93FFFA948882AC84A1C4D6AF0F512B66E2182` |
| `CHMarkDown-1.0.0-win-x64.zip` | 127.75 MiB | `6DCF3BAC069EB41A0B900732E857094D2E38F54A3A15247B0FD0ECBCC79B7279` |

### 已知边界

- 不提供登录、云同步、协作、AI 写作或在线插件市场。
- 不提供 PDF、HTML 或 Word 导出；当前导出格式为 Markdown 或图片 ZIP。
- 不主动下载远程图片，导出时保留远程 URL。
- 暂不检测其他程序同时修改外部 Markdown 文件造成的内容冲突。

更细的逐版本目标、实现和验证记录见 [`docs/ROADMAP.md`](docs/ROADMAP.md)。
